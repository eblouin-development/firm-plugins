"""The `webapp` Django app — this block's server-rendered, HTMX-enhanced
demo surface. NOT vendored: this whole app is new glue authored for this
block (see the block README's "Contents" for the full file map). Sits
alongside `backend/django`'s `core` app once materialized into the same
`apps/api/` directory — see the block README's "Placement & composition
rule"."""
